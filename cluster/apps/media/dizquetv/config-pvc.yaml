---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dizquetv-config-v4
  namespace: media
  labels:
    pv-backup/enabled: true
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block-retain
