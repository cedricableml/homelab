---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config-v4
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: ceph-block-retain
