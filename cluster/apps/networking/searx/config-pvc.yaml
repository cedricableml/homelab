---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: searx-config-v1
  namespace: networking
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
