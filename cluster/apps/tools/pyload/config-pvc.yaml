---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pyload-config-v1
  namespace: tools
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block-retain
