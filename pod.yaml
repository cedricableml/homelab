# ---
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: test
#   namespace: default
# spec:
#   storageClassName: ceph-block
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 100Mi
# ---
# apiVersion: v1
# kind: Pod
# metadata:
#   name: test
#   namespace: default
# spec:
#   nodeSelector:
#     kubernetes.io/hostname: homelab-talos-0-worker-0
#   containers:
#     - name: myapp
#       image: "ubuntu"
#       securityContext:
#         privileged: true
#       args:
#         - "sleep"
#         - "99999999999"
#       volumeMounts:
# #        - name: rook
# #          mountPath: /rook
#         - name: test
#           mountPath: /test
#   volumes:
# #    - name: rook
# #      hostPath:
# #        path: /var/lib/rook
#     - name: test
#       persistentVolumeClaim:
#         claimName: test
#   restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: test
  namespace: rook-ceph
spec:
  selector:
    app: rook-ceph-mgr
    ceph_daemon_id: a
    rook_cluster: rook-ceph
  type: LoadBalancer
  ports:
    - name: test
      protocol: TCP
      port: 8443
      targetPort: 8443
  loadBalancerIP: 10.250.13.101
